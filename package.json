{
  "name": "qalahub",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@11",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "npm run build -w @qalahub/shared",
    "dev": "concurrently -n web,api,worker -c auto \"npm run dev -w @qalahub/web\" \"npm run dev -w @qalahub/api\" \"npm run dev -w @qalahub/worker\"",
    "build": "npm run build -w @qalahub/shared && npm run prisma:generate -w @qalahub/db && npm run build -w @qalahub/api && npm run build -w @qalahub/worker && npm run build -w @qalahub/web",
    "test": "npm run build -w @qalahub/shared && npm run test -w @qalahub/shared",
    "db:generate": "npm run prisma:generate -w @qalahub/db",
    "db:migrate": "npm run prisma:migrate -w @qalahub/db",
    "db:studio": "npm run prisma:studio -w @qalahub/db"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "overrides": {
    "deepmerge-ts": "^8.0.1"
  }
}
